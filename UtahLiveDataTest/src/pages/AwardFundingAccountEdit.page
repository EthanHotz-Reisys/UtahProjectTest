<apex:page standardController="AwardFundingAccount__c" showHeader="false" standardStylesheets="false" extensions="AwardFundingAccountEditCtrl" sidebar="false">
    
    <!-- Including JS and CSS resources -->
    <c:ExternalLibrary />   
          <!--<div  class="bodyDiv bs container-fluid" style="padding-left:30px; float:left; clear:both; width:100%;height:100%;">   --> 
       <!--      <GNT:DynamicLayout templateName="{!pageTemplateName}" recordId="{!recordId}" layoutType="Edit" flexTableParameters="{!keyValueMap}"
              createDefaultValues="{!listDefaultValues}" saveURL="{!savePage}" cancelURL="{!cancelPage}" HideSidebar="true"/>  -->
              <apex:composition template="GNT__ModalTemplate">
              <apex:define name="ModalContent"> 

              <GNT:ModalFlexEditLayout templateName="{!pageTemplateName}" recordId="{!recordId}" layoutType="Edit" flexTableParameters="{!keyValueMap}"
              createDefaultValues="{!listDefaultValues}" saveURL="{!savePage}" cancelURL="{!cancelPage}" HideSidebar="true"/>
              </apex:define>
              </apex:composition>

          <!--</div>-->
          
          <script>
        var j$ = jQuery.noConflict();
        j$(document).ready(function(){
            //buildLookupIcon();
        });

        var buildLookupIcon = function(){
            j$('.lookupIcon').each(function(){
                var currentNode = j$(this);
                currentNode.parent().append('<span class="glyphicon glyphicon-search"></span>');
            });
        }
        
    </script>
    
</apex:page>