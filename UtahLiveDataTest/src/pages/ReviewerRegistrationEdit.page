<apex:page standardController="UserRegistration__c" showHeader="false" extensions="ReviewerRegistrationEditCtrl" standardStylesheets="false" sidebar="false">
   <style>
    #sideBarDivId{
        min-height: 0px !important;
    }
    #body{
        width: 100% !important;
    }
    </style>
    
    <!--<script>    
        var j$ = jQuery.noConflict();
    </script> -->
    <!-- Including JS and CSS resources -->
 <apex:outputPanel rendered="{!showCaptchaNot}"> 
   <c:ExternalLibrary />
   <apex:composition template="GNT__PageTemplate">
        <apex:define name="content"> 
        
                <GNT:FlexEditLayout templateName="{!pageTemplateName}" recordId="{!recordId}" layoutType="Edit" saveURL="{!savePage}" cancelURL="{!cancelPage}" HideSidebar="true" createDefaultValues="{!listDefaultValues}"/>
           
          </apex:define>
    </apex:composition> 
    </apex:outputPanel> 
   <apex:outputPanel rendered="{!showCaptcha}">
   <!--  <apex:include pageName="tmpCapcha"/> -->
    </apex:outputPanel> 
</apex:page>