<apex:page cache="false" standardController="ProgressReports__c" showHeader="false" standardStylesheets="false" extensions="ProgressReportEditCtrl" sidebar="false">
    
    <!-- Including JS and CSS resources -->
    <c:ExternalLibrary />
    <apex:composition template="GNT__PageTemplate">
        <apex:define name="content">
            <GNT:FlexEditLayout templateName="ProgressReportTemplate" packageId="{!packageId}" recordId="{!recordId}" layoutType="Edit" flexTableParameters="{!keyValueMap}" createDefaultValues="{!listDefaultValues}" saveURL="{!savePage}" cancelURL="{!cancelPage}" formTypes="{!formTypes}"/>
        </apex:define>
    </apex:composition>
    
</apex:page>