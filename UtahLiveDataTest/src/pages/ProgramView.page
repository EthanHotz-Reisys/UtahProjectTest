<apex:page standardController="Program__c" title="Program View" extensions="ProgramViewCtrl" sidebar="false" showHeader="false" standardStylesheets="false" action="{!updateStatus}">
  
    <!-- Including JS and CSS resources <c:ExternalLibrary /> -->
    <c:ExternalLibrary />
    
    
    <apex:composition template="GNT__PageTemplate">
        <apex:define name="content">    
            <GNT:FlexViewLayout templateName="{!pageTemplateName}" recordId="{!recordId}" layoutType="View" flexTableParameters="{!keyValueMap}" chartParameters="{!chartParameters}" HideSideBar="{!hideSideBar}" hideMenuBar="{!hideSideBar}" />
            <!-- 
            <GNT:FlexViewLayout templateName="{!pageTemplateName}" recordId="{!recordId}" layoutType="View" flexTableParameters="{!keyValueMap}" chartParameters="{!chartParameters}"/>            
             -->            
        </apex:define>
    </apex:composition> 
   
    
</apex:page>