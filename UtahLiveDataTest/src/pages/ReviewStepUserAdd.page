<apex:page controller="ReviewStepUserAddCtrl" sidebar="false" showHeader="false" standardStylesheets="false">
	
	<!-- Including JS and CSS resources 
	<c:ExternalLibrary />
	<apex:composition template="GNT__PhaseTemplate">
		<apex:define name="body">
			<apex:form >
				<GNT:PageMessages />
				<div class="bs container-fluid">
					<div class="panel panel-default">
						<div class="panel-heading">
							<span class="bold-text">
								<h3 class="panel-title">Select Reviewers - {!reviewStep.Name}</h3>
							</span>
							<span class="button-string">
								<apex:commandbutton action="{!addToReviewStep}"	value="Add to Review Step" styleClass="customBtn"
									rendered="{!lstReviewStepUsersWrapper.size>0}" />
								<apex:commandbutton action="{!Cancel}" value="Cancel" styleClass="customBtn"
									rendered="{!lstReviewStepUsersWrapper.size>0}" />
							</span>
						</div>
						<div class="panel-body panel-body-ext">
							<apex:outputPanel rendered="{!lstReviewStepUsersWrapper.size>0}">
								<div class="row">
									<div class="col-md-12">
										<apex:dataTable value="{!lstReviewStepUsersWrapper}"
											var="reviewer" styleClass="table"
											rendered="{! lstReviewStepUsersWrapper.size>0}">
											<apex:column >
												<label for="" class="508Label" style="display: none;">
													inptCheckbox </label>
												<apex:facet name="header">Select</apex:facet>
												<apex:inputCheckbox id="chkbox"
													value="{!reviewer.selected}" styleClass="508Input" />
											</apex:column>
											<apex:column >
												<apex:facet name="header">Name</apex:facet>
												<apex:outputText value="{!reviewer.user.Name}" />
											</apex:column>
											<apex:column >
												<apex:facet name="header">Organization</apex:facet>
												<apex:outputText value="{!reviewer.user.OrganizationAccountName__c}" />
											</apex:column>
											<apex:column >
												<apex:facet name="header">Email</apex:facet>
												<apex:outputText value="{!reviewer.user.Email}" />
											</apex:column>
											<apex:column >
												<apex:facet name="header">Phone</apex:facet>
												<apex:outputText value="{!reviewer.user.Phone}" />
											</apex:column>
										</apex:dataTable>
									</div>
								</div>
							</apex:outputPanel>
							<apex:outputpanel rendered="{!lstReviewStepUsersWrapper.size==0}">
								<p class="text-center">
									<apex:outputText value="There are no Reviewers available to be added." rendered="{!lstReviewStepUsersWrapper.size==0}" />
									<br />
									<apex:commandbutton action="{!Cancel}" value="Click to go back to Review Step" rendered="{! lstReviewStepUsersWrapper.size==0}"
										styleClass="customBtn" />
								</p>
							</apex:outputpanel>
						</div>
					</div>
				</div>
			</apex:form>
		</apex:define>
	</apex:composition>
	-->
</apex:page>