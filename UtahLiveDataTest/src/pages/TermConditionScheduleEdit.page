<apex:page showHeader="false" standardStylesheets="false" controller="TermConditionScheduleEditCtrl" sidebar="false">
    
    <!-- Including JS and CSS resources -->
    <c:ExternalLibrary />
    <apex:composition template="GNT__ModalTemplate">
        <apex:define name="ModalContent">
            <GNT:ModalFlexEditLayout templateName="{!pageTemplateName}" recordId="{!recordId}" layoutType="Edit" flexTableParameters="{!keyValueMap}" hideCancelBtn="true" 
              createDefaultValues="{!listDefaultValues}" saveURL="{!savePage}" cancelURL="{!cancelPage}"/>
        </apex:define>

    </apex:composition>
    
</apex:page>