<apex:page standardController="Closeout__c" showHeader="false" standardStylesheets="false" extensions="CloseoutRequestEditCtrl" sidebar="false">
    
    <!-- Including JS and CSS resources -->
    <c:ExternalLibrary />
    <apex:composition template="GNT__PageTemplate">
        <apex:define name="content">
            <GNT:FlexEditLayout templateName="{!pageTemplateName}"  packageId="{!packageId}" recordId="{!recordId}" layoutType="Edit" flexTableParameters="{!keyValueMap}"
              createDefaultValues="{!listDefaultValues}" saveURL="{!savePage}" cancelURL="{!cancelPage}"/>
        </apex:define>
    </apex:composition>
    
</apex:page>