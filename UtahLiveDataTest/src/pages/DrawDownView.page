<apex:page standardController="Drawdown__c" extensions="DrawDownViewCtrl" 
            sidebar="false" showHeader="false" standardStylesheets="false">

    <!-- Include JS and CSS resources -->
    <c:ExternalLibrary />

    <apex:composition template="GNT__PageTemplate">
        <apex:define name="content">
            <GNT:FlexViewLayout templateName="{!pageTemplateName}" recordId="{!recordId}" layoutType="View" 
                                        flexTableParameters="{!keyValueMap}" />
        </apex:define>
    </apex:composition>

</apex:page>