@isTest
private class TestStrategicPlanProgramViewCtrl{
   
      static testMethod void testController() {  
      /*
          UserTriggerHelper.skipTrigger=true;
          User u = TestHelper.getAdminUser(true); 
        
          System.runAs(u){
            
             AccountTriggerHelper.skipAll = true;
             StrategicPlan__c stratPlan = TestHelper.getStrategicPlan(true);
          
              Test.startTest();
              ApexPages.StandardController sc = new ApexPages.standardController(stratPlan);
              ApexPages.CurrentPage().getParameters().put('id',stratPlan.Id);
              ApexPages.CurrentPage().getParameters().put('StrategicPlanid',stratPlan.Id);
              StrategicPlanProgramViewCtrl ctrl= new StrategicPlanProgramViewCtrl(sc);
              ctrl.deleteURL = new PageReference('myPage.TestPage');
              ctrl.getKeyValueMap();
              Test.stopTest();
        }
    */
   }
   
}